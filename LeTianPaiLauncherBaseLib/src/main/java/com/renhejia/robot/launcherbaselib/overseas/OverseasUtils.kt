package com.renhejia.robot.launcherbaselib.overseas

import android.content.Context

object OverseasUtils {
    /**
     * 是否为国际化版本
     *
     * @param context
     * @return
     */
    fun isInternationalized(context: Context?): Boolean {
        return false
    }
}
